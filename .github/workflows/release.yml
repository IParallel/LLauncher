name: Build & Release (Windows & Linux)

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build Wails app
        run: |
          wails build -clean -ldflags "-X main.ZIP_PASSWORD=${{ secrets.ZIP_PASSWORD }}"
        env:
          CGO_ENABLED: 0

      - name: Upload EXE to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/bin/*.exe

  build-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build Wails app
        run: |
          wails build -clean -tags webkit2_41 -ldflags "-X main.ZIP_PASSWORD=${{ secrets.ZIP_PASSWORD }}"
        env:
          CGO_ENABLED: 1

      - name: Upload Linux binary to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/bin/*
